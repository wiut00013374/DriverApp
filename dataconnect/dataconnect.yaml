specVersion: "v1beta"
serviceId: "driverapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "driverapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
